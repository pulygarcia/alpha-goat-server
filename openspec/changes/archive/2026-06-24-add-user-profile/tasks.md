## 1. Follows counts

- [x] 1.1 Add `countFollowers(userId)` and `countFollowing(userId)` to `FollowToggler` (repository `count` by `followingId` / `followerId`)
- [x] 1.2 Specs for both count methods (0 and N cases)
- [x] 1.3 Ensure `FollowsModule` exports `FollowToggler` (already exported — verified)

## 2. User lookup by username

- [x] 2.1 Add `byUsernameOrFail` to `UserFinder` (throws `NotFoundException` when missing) — reuses existing `byUsername` instead of a new finder class
- [x] 2.2 Spec: resolves existing user; throws `NotFoundException` when absent

## 3. ProfileResponseDto

- [x] 3.1 Create `ProfileResponseDto` with `id`, `username`, `avatarUrl`, `role`, `createdAt`, `followersCount`, `followingCount`, `reviewsCount`, `isFollowing`, optional `email` (+ `@ApiProperty`)
- [x] 3.2 `from(user, extra)`: set `email` only when `extra.includeEmail`; set `isFollowing = null` when own profile
- [x] 3.3 Spec: email included only for owner; isFollowing null/true/false branches

## 4. Endpoint wiring

- [x] 4.1 Add `GET /users/by-username/:username` to `UsersController` with `OptionalJwtAuthGuard`; business logic extracted to `UserProfileAssembler` (counts via `FollowToggler` + review repo count, `isFollowing`, `includeEmail = viewer is owner`) to keep the controller thin
- [x] 4.2 Wire `UsersModule` ↔ `FollowsModule` with `forwardRef` (cycle), register `Review` in `forFeature`, provide `UserProfileAssembler`
- [x] 4.3 Controller spec: authenticated viewer (forwards id), anonymous (undefined viewer). Assembler spec covers: anon (isFollowing false, no email), other viewer, owner (email + isFollowing null), follower (isFollowing true), 404

## 5. Verification

- [x] 5.1 `npm run test:cov` → 255 passed; touched files at 100%; global 94.35/88.34 (>85). `npm run lint` + `npm run build` green
- [x] 5.2 Swagger: endpoint + `ProfileResponseDto` exposed via `@ApiTags`/`@ApiProperty` decorators (auto-generated by DocumentBuilder)
